import React,{useContext,useEffect,useMemo,useState}from"react";import{Alert,Badge,Button,Card,Col,Container,Row}from"react-bootstrap";import{useLocation,useNavigate,useParams}from"react-router-dom";import NavlogComponent from"../../components/NavlogComponent";import ProcessingIndicatorComponent from"../../components/ProcessingIndicatorComponent";import{AuthContext}from"../../context/AuthContext";import eventService from"../../services/EventService";import cutinappService from"../../services/CutinappService";import{storageUrl}from"../../config";
const formatDate=v=>v?new Intl.DateTimeFormat("pt-BR",{dateStyle:"long",timeStyle:"short"}).format(new Date(v)):"Data não informada";
export default function EventViewPage(){const{slug}=useParams(),navigate=useNavigate(),location=useLocation(),{user}=useContext(AuthContext);const[data,setData]=useState(null),[loading,setLoading]=useState(true),[claimingId,setClaimingId]=useState(null),[error,setError]=useState(""),[success,setSuccess]=useState("");useEffect(()=>{let active=true;eventService.view(slug).then(r=>active&&setData(r)).catch(e=>active&&setError(e?.message||"Não foi possível carregar este evento.")).finally(()=>active&&setLoading(false));return()=>{active=false}},[slug]);const event=data?.event||null,tickets=useMemo(()=>(data?.tickets||event?.tickets||[]).filter(t=>Number(t.price)===0),[data,event]),isOwner=Boolean(event?.production?.user_id&&Number(event.production.user_id)===Number(user?.id));const claim=async ticket=>{if(!user){navigate("/login",{state:{from:location.pathname}});return}setClaimingId(ticket.id);setError("");setSuccess("");try{await cutinappService.claimCourtesy(ticket.id);setSuccess("Cortesia retirada. Seu QR Code já está disponível em Meus ingressos.");window.setTimeout(()=>navigate("/passes"),650)}catch(e){setError(e?.message||"Não foi possível retirar esta cortesia.")}finally{setClaimingId(null)}};return <div className="cut-app-page"><NavlogComponent/>{(loading||claimingId)&&<ProcessingIndicatorComponent label={claimingId?"Reservando cortesia":"Carregando evento"}/>} {!loading&&event&&<><section className="cut-event-hero" style={event.image?{backgroundImage:`linear-gradient(180deg,rgba(3,10,16,.18),rgba(3,10,16,.96)),url(${storageUrl}${event.image})`}:undefined}><Container className="cut-page-container"><div className="cut-event-hero__content"><Badge bg="success" className="mb-3">Evento Cutinapp</Badge><h1>{event.title}</h1><p>{formatDate(event.start_date)}</p><span>{event.venue||event.address}</span><span>{event.production?.name?`Por ${event.production.name}`:""}</span></div></Container></section><Container className="cut-page-container py-4 py-lg-5">{error&&<Alert variant="danger">{error}</Alert>}{success&&<Alert variant="success">{success}</Alert>}<Row className="g-4"><Col lg={8}><Card className="cut-panel mb-4"><Card.Body className="p-4 p-lg-5"><span className="cut-eyebrow">Sobre o evento</span><h2 className="cut-section-title mt-2">Informações</h2><p className="cut-body-copy">{event.description}</p><div className="cut-event-details"><div><i className="fa-regular fa-calendar"/><span><strong>Início</strong>{formatDate(event.start_date)}</span></div><div><i className="fa-solid fa-location-dot"/><span><strong>Local</strong>{event.venue||event.address}</span></div>{event.city&&<div><i className="fa-solid fa-map"/><span><strong>Cidade</strong>{event.city}{event.uf?` - ${event.uf}`:""}</span></div>}{event.production?.name&&<div><i className="fa-solid fa-bullhorn"/><span><strong>Produção</strong>{event.production.name}</span></div>}</div></Card.Body></Card></Col><Col lg={4}><Card className="cut-panel"><Card.Body className="p-4"><span className="cut-eyebrow">Entrada</span><h2 className="cut-section-title mt-2">Cortesias disponíveis</h2>{tickets.length===0?<div className="cut-empty-state-inline"><p>Nenhuma cortesia gratuita disponível neste momento.</p>{isOwner&&<Button onClick={()=>navigate(`/ticket/create?eventId=${event.id}`)}>Criar cortesia</Button>}</div>:<div className="cut-ticket-list">{tickets.map(ticket=><div className="cut-ticket-option" key={ticket.id}><div><strong>{ticket.name}</strong><span>Grátis · lote com {ticket.quantity} unidades</span></div><Button onClick={()=>claim(ticket)} disabled={claimingId===ticket.id}>{user?"Retirar cortesia":"Entrar para retirar"}</Button></div>)}</div>}{isOwner&&<div className="cut-owner-actions mt-4"><Button variant="outline-light" onClick={()=>navigate(`/event/edit/${event.id}`)}>Editar evento</Button><Button variant="outline-light" onClick={()=>navigate("/checkin")}>Abrir portaria</Button></div>}</Card.Body></Card></Col></Row></Container></>} {!loading&&!event&&<Container className="cut-page-container py-5"><Alert variant="danger">{error||"Evento não encontrado."}</Alert></Container>}</div>}
