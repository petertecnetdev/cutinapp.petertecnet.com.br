import React from "react";

/**
 * Legacy compatibility component.
 * Old pages still import NavlogComponent, but navigation is now provided
 * centrally by CutinLayout at the route level so every page shares the
 * same responsive Peter Tecnet/Rasoio shell.
 */
const Navigation = () => null;

export default Navigation;
